{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "django makemigrations",
            "type": "shell",
            "command": "python backend/manage.py makemigrations survey",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
}
